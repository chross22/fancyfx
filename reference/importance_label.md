# Axis label naming the importance metric

Axis label naming the importance metric

## Usage

``` r
importance_label(metric)
```

## Arguments

- metric:

  `"auc"` or `"rmse"`.

## Value

The label to show.
