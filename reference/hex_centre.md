# Centre of a hexagon from its axial coordinates

Centre of a hexagon from its axial coordinates

## Usage

``` r
hex_centre(q, r, size)
```

## Arguments

- q, r:

  Axial coordinates.

- size:

  Hexagon size, centre to vertex.

## Value

A list with `x` and `y`.
