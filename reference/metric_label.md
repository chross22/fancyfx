# Display name for a metric

Display name for a metric

## Usage

``` r
metric_label(metric)
```

## Arguments

- metric:

  One of `"tss"`, `"sensitivity"`, `"specificity"`.

## Value

The label to show.
