# The model's response, as stored in newdata

The model's response, as stored in newdata

## Usage

``` r
model_outcome(model, newdata)
```

## Arguments

- model:

  A fitted model.

- newdata:

  Data containing the response.

## Value

The response vector.
