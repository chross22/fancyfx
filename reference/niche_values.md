# Pull comparable numeric vectors out of the accepted inputs

Pull comparable numeric vectors out of the accepted inputs

## Usage

``` r
niche_values(x, y)
```

## Arguments

- x, y:

  Two rasters or two numeric vectors.

## Value

A list with `x` and `y`.
