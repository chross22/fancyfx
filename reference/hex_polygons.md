# The six corners of each hexagon, as polygon rows

The six corners of each hexagon, as polygon rows

## Usage

``` r
hex_polygons(centres, size)
```

## Arguments

- centres:

  A data frame with `.x` and `.y`.

- size:

  Hexagon size, centre to vertex.

## Value

A data frame of vertices with a `.id` per hexagon.
