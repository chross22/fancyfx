# Format one or several AUC values for display

Format one or several AUC values for display

## Usage

``` r
auc_label(auc.value)
```

## Arguments

- auc.value:

  One AUC, or one per fold.

## Value

A single label string.
