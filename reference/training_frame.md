# Training covariate values, from a data frame or a fitted model

Training covariate values, from a data frame or a fitted model

## Usage

``` r
training_frame(training)
```

## Arguments

- training:

  A data frame, or a fitted model to take the frame from.

## Value

A data frame.
