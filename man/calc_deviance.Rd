% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brt.R
\name{calc_deviance}
\alias{calc_deviance}
\title{Deviance of a set of predictions}
\usage{
calc_deviance(
  observed,
  predicted,
  family = c("binomial", "poisson", "gaussian", "laplace"),
  weights = NULL,
  mean = TRUE
)
}
\arguments{
\item{observed}{Observed outcomes.}

\item{predicted}{Predicted values, on the response scale.}

\item{family}{\code{"binomial"}, \code{"poisson"}, \code{"gaussian"}, or \code{"laplace"}.}

\item{weights}{Optional observation weights.}

\item{mean}{Whether to return the mean deviance per observation rather than
the total.}
}
\value{
A single number.
}
\description{
How badly the predictions miss, on the scale the model was fitted on. Lower
is better, and zero is a perfect fit.
}
\details{
Deviance answers a question AUC cannot: AUC only cares about ranking, and
will not notice predictions that are ordered correctly but wrong. Deviance
penalises confident mistakes hardest, which is usually the failure that
matters.

It is most useful as a \emph{relative} measure. Compare a model's deviance to the
null deviance -- what you would get predicting the overall mean for every
observation -- and the ratio is the proportion of deviance explained, the
quantity boosted regression tree work reports as a matter of course.
}
\examples{
set.seed(1)
dat <- data.frame(x = runif(300, 1, 10))
dat$y <- rbinom(300, 1, plogis(-3 + 0.6 * dat$x))
fit <- glm(y ~ x, data = dat, family = binomial)

fitted.deviance <- calc_deviance(dat$y, fitted(fit), "binomial")
null.deviance <- calc_deviance(dat$y, rep(mean(dat$y), nrow(dat)),
                               "binomial")

# Proportion of deviance explained
1 - fitted.deviance / null.deviance

}
\references{
Elith, J., Leathwick, J. R., & Hastie, T. (2008). A working guide to boosted
regression trees. \emph{Journal of Animal Ecology}, 77(4), 802-813.
\doi{10.1111/j.1365-2656.2008.01390.x}
}
\seealso{
\code{\link[=threshold_metrics]{threshold_metrics()}} for discrimination,
\code{\link[=calibration_estimates]{calibration_estimates()}} for whether the probabilities are honest.

Other evaluation plots:
\code{\link[=calibration_estimates]{calibration_estimates()}},
\code{\link[=permutation_importance]{permutation_importance()}},
\code{\link[=plotCalibration]{plotCalibration()}},
\code{\link[=plotImportance]{plotImportance()}},
\code{\link[=plotROC]{plotROC()}},
\code{\link[=plotThreshold]{plotThreshold()}},
\code{\link[=spatial_sorting_bias]{spatial_sorting_bias()}},
\code{\link[=threshold_metrics]{threshold_metrics()}}
}
\concept{evaluation plots}
