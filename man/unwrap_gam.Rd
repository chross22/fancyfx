% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effect_estimates.R
\name{unwrap_gam}
\alias{unwrap_gam}
\title{Unwrap a fitted object that carries its GAM in a list element}
\usage{
unwrap_gam(model)
}
\arguments{
\item{model}{A fitted model, wrapped or not.}
}
\value{
The \verb{$gam} element when there is one, otherwise \code{model} unchanged.
}
\description{
\code{gamm4::gamm4()} and \code{mgcv::gamm()} return a list holding the GAM alongside
the mixed-model fit it was estimated through, rather than a fitted model
object. Nothing downstream can use the wrapper: \code{marginaleffects} refuses the
class outright, and \code{formula()} and \code{predict()} both fail on it. The \verb{$gam}
element is an ordinary \code{gam}, so unwrapping makes every path work at once.
}
\details{
The random effects are left behind with the wrapper. That is the right
default and matches the rest of the package -- the smooth is reported at the
population level -- but it does mean the ribbon covers uncertainty in the
smooth alone.
}
\keyword{internal}
