% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{plotCalibration}
\alias{plotCalibration}
\title{Plot a calibration curve with a rug of where predictions fall}
\usage{
plotCalibration(
  model,
  newdata,
  bins = 10,
  binning = c("quantile", "width"),
  folds = NULL,
  level = 0.95,
  title = "",
  show.stats = TRUE,
  rug.type = c("histogram", "density"),
  rug.bins = 30,
  theme = theme_fancyfx(),
  palette = fancyfx_palette(),
  colour = fancyfx_palette(1),
  ...
)
}
\arguments{
\item{model}{A fitted presence/absence model.}

\item{newdata}{Data to evaluate on. Required; see \code{\link[=threshold_metrics]{threshold_metrics()}}.}

\item{bins}{Number of bins.}

\item{binning}{\code{"quantile"} (the default) or \code{"width"}.}

\item{folds}{Optional fold identifiers, one per row of \code{newdata}.}

\item{level}{Level for the interval on each bin.}

\item{title}{Plot title, optional.}

\item{show.stats}{Whether to report the calibration intercept and slope on
the plot.}

\item{rug.type}{Type of rug drawn above the curve.}

\item{rug.bins}{Number of bins for a histogram rug.}

\item{theme}{A \pkg{ggplot2} theme. Defaults to \code{\link[=theme_fancyfx]{theme_fancyfx()}}.}

\item{palette}{Colours used for fold curves. Defaults to
\code{\link[=fancyfx_palette]{fancyfx_palette()}}.}

\item{colour}{Colour of the curve when there are no folds.}

\item{...}{Passed to \code{\link[=calibration_estimates]{calibration_estimates()}} and on to \code{\link[stats:predict]{stats::predict()}}.}
}
\value{
A \code{patchwork} object: the rug above, the calibration curve below.
}
\description{
Binned predictions against observed frequency, with the diagonal as the
reference: a model that says 0.7 should be right about 70\% of the time. The
rug above shows where the predictions actually are, which is what tells you
whether a bin near the extremes is worth reading at all.
}
\details{
The rug is the same idea as in \code{\link[=plotEffects]{plotEffects()}}, and matters more here than
almost anywhere else. Calibration is usually worst at the extremes, and the
extremes are usually where the fewest predictions are, so the most
eye-catching departures from the diagonal are often the least trustworthy
points on the plot. The rug shows that directly, and the interval on each
bin shows it again.

See \code{\link[=calibration_estimates]{calibration_estimates()}} for why quantile binning is the default, and
for what the reported intercept and slope mean.
}
\examples{
set.seed(1)
dat <- data.frame(x1 = runif(600, 1, 10), x2 = runif(600, 1, 10))
dat$y <- rbinom(600, 1, plogis(-3 + 0.6 * dat$x1))

fit <- glm(y ~ x1 + x2, data = dat[1:300, ], family = binomial)
plotCalibration(fit, dat[301:600, ])

}
\seealso{
\code{\link[=calibration_estimates]{calibration_estimates()}} for the numbers, \code{\link[=plotROC]{plotROC()}} for
discrimination, which is a different question.

Other evaluation plots:
\code{\link[=calibration_estimates]{calibration_estimates()}},
\code{\link[=permutation_importance]{permutation_importance()}},
\code{\link[=plotImportance]{plotImportance()}},
\code{\link[=plotROC]{plotROC()}},
\code{\link[=plotThreshold]{plotThreshold()}},
\code{\link[=threshold_metrics]{threshold_metrics()}}
}
\concept{evaluation plots}
