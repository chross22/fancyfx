% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brt.R, R/effect_estimates.R
\name{effect_estimates.gbm}
\alias{effect_estimates.gbm}
\alias{effect_estimates}
\alias{effect_estimates.gam}
\alias{effect_estimates.scam}
\alias{effect_estimates.gamm4}
\alias{effect_estimates.gamm}
\alias{effect_estimates.default}
\title{Extract a variable's effect from a model as a tidy data frame}
\usage{
\method{effect_estimates}{gbm}(
  model,
  var,
  scale = c("auto", "link", "response"),
  interval = c("auto", "se", "ci", "cri"),
  level = 0.95,
  n = 100,
  data = NULL,
  n.trees = NULL,
  ...
)

effect_estimates(
  model,
  var,
  scale = c("auto", "link", "response"),
  interval = c("auto", "se", "ci", "cri"),
  level = 0.95,
  n = 100,
  data = NULL,
  ...
)

\method{effect_estimates}{gam}(
  model,
  var,
  scale = c("auto", "link", "response"),
  interval = c("auto", "se", "ci", "cri"),
  level = 0.95,
  n = 100,
  ...
)

\method{effect_estimates}{scam}(
  model,
  var,
  scale = c("auto", "link", "response"),
  interval = c("auto", "se", "ci", "cri"),
  level = 0.95,
  n = 100,
  ...
)

\method{effect_estimates}{gamm4}(model, var, ...)

\method{effect_estimates}{gamm}(model, var, ...)

\method{effect_estimates}{default}(
  model,
  var,
  scale = c("auto", "link", "response"),
  interval = c("auto", "se", "ci", "cri"),
  level = 0.95,
  n = 100,
  data = NULL,
  re.form = NA,
  ...
)
}
\arguments{
\item{model}{A fitted model.}

\item{var}{Name of the predictor whose effect to extract, as a string.}

\item{scale}{\code{"auto"} (the default), \code{"link"}, or \code{"response"}. \code{"auto"}
resolves to whichever is natural for the backend -- \code{"link"} for a GAM
partial effect, \code{"response"} for predictions. See Details: for GAMs this
chooses between two genuinely different quantities, not two axis scales.}

\item{interval}{\code{"auto"} (the default), \code{"se"} for a \verb{+/- 1} standard error
ribbon, or \code{"ci"} for an interval at \code{level}. \code{"auto"} gives the SE ribbon
except for Bayesian fits, which report no standard error and get their
credible interval. \code{"cri"} is another name for \code{"ci"}.}

\item{level}{Interval level, used when \code{interval = "ci"}.}

\item{n}{Number of points at which to evaluate the effect.}

\item{data}{Optional data frame to take the predictor's range from, for
models that do not keep the data they were fitted on -- a \code{gbm}, for
instance. \code{\link[=plotEffects]{plotEffects()}} passes the \code{dat} it was given.}

\item{n.trees}{For a boosted regression tree, how many trees to use.
Defaults to every tree in the fit, which is rarely what you want -- pass
the number \code{\link[gbm:gbm.perf]{gbm::gbm.perf()}} selected.}

\item{...}{Passed to the underlying backend
(\code{\link[gratia:smooth_estimates]{gratia::smooth_estimates()}} or \code{\link[marginaleffects:predictions]{marginaleffects::predictions()}}).}

\item{re.form}{For a mixed model, which random effects to include. \code{NA} (the
default) gives the population-level effect; \code{NULL} includes all of them.
Not forwarded to models without random effects, which would reject it.
Note that standard errors cover the fixed effects only either way.}
}
\value{
A data frame with four columns -- \code{.x}, \code{.estimate}, \code{.lower},
\code{.upper} -- and a \code{"quantity"} attribute naming what was computed, which
\code{\link[=plotEffects]{plotEffects()}} uses to label the y axis.
}
\description{
The one place in the package that knows anything about model classes.
Everything downstream -- the transforms, the ribbon, the rug, the panel
arranging -- works off the standardized frame this returns, so adding support
for a new kind of model means writing a method here and nothing else.
}
\details{
Exported both because the tidy frame is useful on its own, and so support
for further model classes can be added from outside the package by writing
an \code{effect_estimates()} method.

Two backends, because the two quantities they produce are not the same thing:
\itemize{
\item \strong{GAMs on the link scale} go to \code{\link[gratia:smooth_estimates]{gratia::smooth_estimates()}}, which
returns the \emph{partial effect} of the smooth: the term's own contribution,
centered so it averages to zero, with the rest of the model excluded. This
is what this package drew before it handled anything but GAMs.
\item \strong{Everything else} goes to \code{\link[marginaleffects:predictions]{marginaleffects::predictions()}}, which returns
\emph{predicted values} -- the model's actual fitted output as \code{var} varies,
with the other predictors held at representative values (means for numeric
predictors, modes for factors).
}

A centered partial effect has no meaningful back-transformation to the
response scale on its own, so asking a GAM for \code{scale = "response"} gives you
predictions rather than an incoherent partial effect. The returned frame
carries a \code{"quantity"} attribute recording which of the two you got, and
\code{\link[=plotEffects]{plotEffects()}} uses it to label the y axis honestly.
}
\examples{
gam.fit <- mgcv::gam(Petal.Length ~ s(Sepal.Length), data = iris)
est <- effect_estimates(gam.fit, "Sepal.Length")
head(est)
attr(est, "quantity")

# The same call against a model of any other class
lm.fit <- lm(Petal.Length ~ Sepal.Length + Species, data = iris)
head(effect_estimates(lm.fit, "Sepal.Length"))

# Useful on its own if you would rather build the plot yourself
ggplot2::ggplot(est, ggplot2::aes(.data$.x, .data$.estimate)) +
  ggplot2::geom_line()

}
\seealso{
\code{\link[=plotEffects]{plotEffects()}}, which turns this into a plot.
}
