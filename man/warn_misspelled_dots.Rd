% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{warn_misspelled_dots}
\alias{warn_misspelled_dots}
\title{Warn about an argument in \code{...} that looks like a misspelled formal}
\usage{
warn_misspelled_dots(dots.names, formals.names)
}
\arguments{
\item{dots.names}{Names of the arguments captured by \code{...}.}

\item{formals.names}{Formal argument names of the calling function.}
}
\value{
Invisibly \code{NULL}, called for its side effect.
}
\description{
\code{...} legitimately carries arguments through to the modelling backends, so
unknown names cannot simply be refused. But that makes a typo silent: a
misspelled \code{rug.type} is passed to \code{gratia}, ignored there, and the plot
comes back with the default rug and no complaint. This catches the case that
matters -- a name that is almost certainly a formal argument spelled wrong --
while leaving genuine backend arguments alone.
}
\details{
Matching is deliberately tight. A name qualifies only if, ignoring case and
any dots or underscores, it either equals a formal argument exactly or is one
character away from one. Anything looser would start flagging real backend
arguments.
}
\keyword{internal}
