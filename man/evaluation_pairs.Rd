% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{evaluation_pairs}
\alias{evaluation_pairs}
\title{Line up observed outcomes against predicted probabilities}
\usage{
evaluation_pairs(
  model,
  newdata,
  folds = NULL,
  require.both.classes = TRUE,
  ...
)
}
\arguments{
\item{model}{A fitted model.}

\item{newdata}{Data to evaluate on.}

\item{folds}{Optional fold identifiers, one per row of \code{newdata}.}

\item{require.both.classes}{Whether to refuse data containing only one
outcome class.}

\item{...}{Passed to \code{\link[stats:predict]{stats::predict()}}.}
}
\value{
A list with \code{observed}, \code{predicted}, \code{folds}, \code{complete} and
\code{in.sample}.
}
\description{
The shared prologue for every evaluation function: unwrap the model, pull the
response out of \code{newdata}, predict, drop incomplete rows, and say something
if the data turns out to be what the model was fitted on. Extracted so the
rules about evaluation data are enforced in one place and cannot drift
between functions.
}
\keyword{internal}
