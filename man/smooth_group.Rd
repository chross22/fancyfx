% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effect_estimates.R
\name{smooth_group}
\alias{smooth_group}
\title{Identify the factor a set of smooths is split by}
\usage{
smooth_group(est)
}
\arguments{
\item{est}{A \code{gratia::smooth_estimates()} frame, as a data frame.}
}
\value{
A factor of level labels, or \code{NULL} when the smooth is not split.
}
\description{
A factor-smooth interaction -- \code{s(x, by = f)} -- is one smooth per level of
\code{f}, and \code{gratia} returns them stacked in a single frame. Without separating
them, \code{geom_line()} joins the end of one level's curve to the start of the
next and draws a zigzag that looks like a single wildly varying smooth.
}
\keyword{internal}
