% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{plotUncertainty}
\alias{plotUncertainty}
\title{Map the disagreement between ensemble members}
\usage{
plotUncertainty(
  x,
  statistic = c("sd", "cv", "range", "iqr"),
  na.rm = FALSE,
  title = "",
  legend.lab = NULL,
  max.cells = 5e+05,
  theme = theme_fancyfx(),
  option = "viridis"
)
}
\arguments{
\item{x}{A \code{SpatRaster} whose layers are ensemble members, or a
single-layer raster already summarised.}

\item{statistic}{Spread statistic, passed to \code{\link[=ensemble_summary]{ensemble_summary()}}. Ignored
when \code{x} has one layer.}

\item{na.rm}{Whether to ignore missing members. Defaults to \code{FALSE}; see
\code{\link[=ensemble_summary]{ensemble_summary()}}.}

\item{title}{Plot title, optional.}

\item{legend.lab}{Legend title. Defaults to naming the statistic.}

\item{max.cells}{Largest number of cells to draw. A raster above this is
aggregated first, and the plot says by how much. See Details.}

\item{theme}{A \pkg{ggplot2} theme. Defaults to \code{\link[=theme_fancyfx]{theme_fancyfx()}}.}

\item{option}{Viridis colour map option, passed to
\code{\link[ggplot2:scale_fill_viridis_c]{ggplot2::scale_fill_viridis_c()}}.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Draws the spread across an ensemble of projections. The companion to a
projection map rather than a replacement for it: the two together say what
the ensemble expects and where it is least sure.
}
\details{
Uncertainty is a magnitude, so it gets a sequential, perceptually uniform
viridis scale rather than a rainbow -- on a rainbow the eye invents
boundaries where the data has none, which on an uncertainty map means
inventing places the ensemble agreed.

Projection rasters are routinely millions of cells, and drawing one cell per
pixel is both slow and pointless at figure size. Above \code{max.cells} the
raster is aggregated by whole-number factors before plotting. That changes
what is on the page, so it is reported in the subtitle rather than done
quietly.
}
\examples{
if (requireNamespace("terra", quietly = TRUE)) {
  set.seed(1)
  r <- terra::rast(nrows = 30, ncols = 30, vals = runif(900))
  ensemble <- c(r, r * 1.2, r * 0.7)
  names(ensemble) <- c("model1", "model2", "model3")

  plotUncertainty(ensemble)
}

}
\seealso{
\code{\link[=ensemble_summary]{ensemble_summary()}} for the raster itself, \code{\link[=plotExtrapolation]{plotExtrapolation()}}
for whether the projection is extrapolating.

Other spatial plots:
\code{\link[=ensemble_summary]{ensemble_summary()}},
\code{\link[=hex_bin]{hex_bin()}},
\code{\link[=mess]{mess()}},
\code{\link[=niche_equivalency]{niche_equivalency()}},
\code{\link[=niche_overlap]{niche_overlap()}},
\code{\link[=plotExtrapolation]{plotExtrapolation()}},
\code{\link[=plotHexbin]{plotHexbin()}},
\code{\link[=thin_points]{thin_points()}}
}
\concept{spatial plots}
