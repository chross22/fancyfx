% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparePlots.R
\name{comparePlots}
\alias{comparePlots}
\title{Compare the same effect across several models}
\usage{
comparePlots(
  models,
  dat,
  var,
  title = "",
  xlab = NULL,
  ylab = NULL,
  transform = c("none", "log", "log10", "sqrt"),
  scale = c("auto", "link", "response"),
  interval = c("se", "ci"),
  level = 0.95,
  n = 100,
  rug.type = c("histogram", "density"),
  bins = 30,
  common.legend = TRUE,
  labels = "A",
  ...
)
}
\arguments{
\item{models}{A list of fitted models. Names become the panel titles; if the
list is unnamed, panels are titled \code{"Model 1"}, \code{"Model 2"}, and so on.
Models need not be of the same class -- a GAM can be compared against a
GLM, subject to the caveat below.}

\item{dat}{Raw data the models were fitted on, for the rugs. One data frame
used for every panel, or a list with one per model.}

\item{var}{Name of the predictor to plot. One name used for every model, or
one per model, for comparing specifications that name a term differently.}

\item{title}{Overall title for the figure, optional.}

\item{xlab}{Label for the x-axis of every panel. Defaults to \code{var}.}

\item{ylab}{Label for the y-axis of every panel. Defaults per panel to the
quantity that panel actually computed.}

\item{transform}{How to transform the variable before plotting. One value
used for every panel, or one per model.}

\item{scale}{\code{"auto"}, \code{"link"}, or \code{"response"}, passed to \code{\link[=plotEffects]{plotEffects()}}.}

\item{interval}{\code{"se"} or \code{"ci"}, passed to \code{\link[=plotEffects]{plotEffects()}}.}

\item{level}{Confidence level used when \code{interval = "ci"}.}

\item{n}{Number of points at which to evaluate each effect.}

\item{rug.type}{Type of rug plot to draw above each effect.}

\item{bins}{Number of bins for a histogram rug.}

\item{common.legend}{Whether the panels share one legend. Worth turning off
when the models are split by different factors, since a shared legend
would then describe only the first.}

\item{labels}{Panel labels: \code{"A"} (the default) for upper-case letters, \code{"a"}
for lower-case, \code{"1"} for numbers, \code{"none"} for none, or a character vector
used verbatim, one per panel.}

\item{...}{Passed through to \code{\link[=plotEffects]{plotEffects()}} and on to the backend.}
}
\value{
The arranged plots, as returned by \code{\link[ggpubr:ggarrange]{ggpubr::ggarrange()}}.
}
\description{
Plots one variable's effect from each of several models, side by side, so
competing specifications can be read against each other. The companion to
\code{\link[=combinePlots]{combinePlots()}}, which holds the model fixed and varies the predictor.
}
\details{
The motivating case is asking what a modelling choice actually bought you:
fit the same data with and without a factor-smooth interaction, put the two
panels next to each other, and see whether the smooths really do differ by
group. Each panel keeps its own rug, so a group with little data is visible
rather than inferred.
}
\section{Comparing like with like}{

Panels are only comparable if they show the same quantity. A GAM defaults to
its partial effect and a GLM to its predictions, and putting those side by
side compares a curve centered on zero against one that is not. When the
models are of different classes, pass \code{scale = "response"} so every panel
reports predictions, or read the y-axis labels carefully -- they will differ,
which is the signal that the panels are not on the same footing.
}

\examples{
# Does letting the smooth vary by species actually buy anything?
plain <- mgcv::gam(Petal.Length ~ s(Sepal.Length), data = iris)
by.species <- mgcv::gam(Petal.Length ~ s(Sepal.Length, by = Species) + Species,
                        data = iris)

comparePlots(list("Single smooth" = plain,
                  "Smooth by species" = by.species),
             iris, "Sepal.Length",
             title = "Is a factor-smooth interaction worth it?")

# Comparing across model classes: ask for the same quantity from both.
comparePlots(list(GAM = plain, Linear = lm(Petal.Length ~ Sepal.Length, iris)),
             iris, "Sepal.Length", scale = "response")

}
\seealso{
\code{\link[=combinePlots]{combinePlots()}} to vary the predictor instead of the model, and
\code{\link[=plotEffects]{plotEffects()}} for a single panel.

Other effect plots:
\code{\link[=combinePlots]{combinePlots()}},
\code{\link[=plotEffects]{plotEffects()}},
\code{\link[=plotRugs]{plotRugs()}},
\code{\link[=plotSmooths]{plotSmooths()}}
}
\concept{effect plots}
