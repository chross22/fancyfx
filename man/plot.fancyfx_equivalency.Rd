% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/niche.R
\name{plot.fancyfx_equivalency}
\alias{plot.fancyfx_equivalency}
\title{Plot a niche equivalency test against its null}
\usage{
\method{plot}{fancyfx_equivalency}(
  x,
  title = "",
  bins = 20,
  theme = theme_fancyfx(),
  colour = fancyfx_palette(1),
  ...
)
}
\arguments{
\item{x}{A result from \code{\link[=niche_equivalency]{niche_equivalency()}}.}

\item{title}{Plot title, optional.}

\item{bins}{Number of histogram bins for the null distribution.}

\item{theme}{A \pkg{ggplot2} theme. Defaults to \code{\link[=theme_fancyfx]{theme_fancyfx()}}.}

\item{colour}{Colour of the observed-value line.}

\item{...}{Ignored.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
The observed overlap on its own says little -- two surfaces built from the
same covariates over the same domain overlap substantially whatever the
species do. What makes it readable is seeing it against the distribution of
overlaps that interchangeable occurrences would have produced.
}
\examples{
set.seed(1)
grid <- seq(0, 20, length.out = 50)
fit_density <- function(o) stats::dnorm(grid, mean(o$temp), stats::sd(o$temp))

result <- niche_equivalency(data.frame(temp = rnorm(60, 8, 1.5)),
                            data.frame(temp = rnorm(60, 14, 1.5)),
                            fit_density, n.rep = 19)
plot(result)

}
\seealso{
\code{\link[=niche_equivalency]{niche_equivalency()}} for the test itself.

Other spatial plots:
\code{\link[=ensemble_summary]{ensemble_summary()}},
\code{\link[=hex_bin]{hex_bin()}},
\code{\link[=mess]{mess()}},
\code{\link[=niche_equivalency]{niche_equivalency()}},
\code{\link[=niche_overlap]{niche_overlap()}},
\code{\link[=plotExtrapolation]{plotExtrapolation()}},
\code{\link[=plotHexbin]{plotHexbin()}},
\code{\link[=plotUncertainty]{plotUncertainty()}},
\code{\link[=thin_points]{thin_points()}}
}
\concept{spatial plots}
