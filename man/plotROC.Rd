% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotROC.R
\name{plotROC}
\alias{plotROC}
\title{Plot a ROC curve}
\usage{
plotROC(
  model,
  newdata,
  folds = NULL,
  title = "",
  show.auc = TRUE,
  theme = theme_fancyfx(),
  palette = fancyfx_palette(),
  linewidth = 0.8,
  ...
)
}
\arguments{
\item{model}{A fitted presence/absence model.}

\item{newdata}{Data to evaluate on. Required, and it should not be the data
the model was fitted to -- see \code{\link[=threshold_metrics]{threshold_metrics()}}.}

\item{folds}{Optional fold identifiers, one per row of \code{newdata}. Each fold
is drawn as its own curve, so the spread is visible rather than averaged.}

\item{title}{Plot title, optional.}

\item{show.auc}{Whether to report the AUC on the plot.}

\item{theme}{A \pkg{ggplot2} theme. Defaults to \code{\link[=theme_fancyfx]{theme_fancyfx()}}.}

\item{palette}{Colours used for fold curves. Defaults to
\code{\link[=fancyfx_palette]{fancyfx_palette()}}.}

\item{linewidth}{Width of the curve.}

\item{...}{Passed to \code{\link[=threshold_metrics]{threshold_metrics()}} and on to \code{\link[stats:predict]{stats::predict()}}.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Sensitivity against the false positive rate across every cutoff, with the
area under the curve reported. The diagonal is the reference: a model that
ranks presences no better than chance lies on it.
}
\details{
The plot is square with equal axes, because a ROC curve read on unequal
axes misleads about how far from the diagonal it sits.

AUC is computed from ranks, not by integrating the drawn curve, so tied
predictions are handled exactly. With \code{folds}, one AUC per fold is reported
as a range rather than a mean: the spread is the informative part.

If the metrics came from the model's own training data, the plot says so
beneath the axis. That annotation is not decoration -- an in-sample ROC can
look excellent for a model with no predictive value at all.
}
\examples{
set.seed(1)
dat <- data.frame(x1 = runif(400, 1, 10), x2 = runif(400, 1, 10))
dat$y <- rbinom(400, 1, plogis(-3 + 0.6 * dat$x1))
train <- dat[1:200, ]
test <- dat[201:400, ]

fit <- glm(y ~ x1 + x2, data = train, family = binomial)
plotROC(fit, test)

}
\references{
Allouche, O., Tsoar, A., & Kadmon, R. (2006). Assessing the accuracy of
species distribution models: prevalence, kappa and the true skill statistic
(TSS). \emph{Journal of Applied Ecology}, 43(6), 1223-1232.
\doi{10.1111/j.1365-2664.2006.01214.x}
}
\seealso{
\code{\link[=plotThreshold]{plotThreshold()}} for choosing a cutoff, \code{\link[=threshold_metrics]{threshold_metrics()}} for
the numbers underneath.

Other evaluation plots:
\code{\link[=calibration_estimates]{calibration_estimates()}},
\code{\link[=permutation_importance]{permutation_importance()}},
\code{\link[=plotCalibration]{plotCalibration()}},
\code{\link[=plotImportance]{plotImportance()}},
\code{\link[=plotThreshold]{plotThreshold()}},
\code{\link[=threshold_metrics]{threshold_metrics()}}
}
\concept{evaluation plots}
