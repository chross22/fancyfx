% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{ensemble_summary}
\alias{ensemble_summary}
\title{Summarise an ensemble of projection rasters}
\usage{
ensemble_summary(
  x,
  statistic = c("sd", "cv", "range", "iqr", "mean", "median"),
  na.rm = FALSE
)
}
\arguments{
\item{x}{A \code{SpatRaster} whose layers are ensemble members.}

\item{statistic}{What to compute across layers at each cell. \code{"sd"},
\code{"cv"}, \code{"range"} and \code{"iqr"} describe disagreement; \code{"mean"} and
\code{"median"} describe the projection itself.}

\item{na.rm}{Whether to ignore members that are missing at a cell. See
Details -- the default is deliberately \code{FALSE}.}
}
\value{
A single-layer \code{SpatRaster}.
}
\description{
Collapses a stack whose layers are ensemble members -- competing models,
emissions scenarios, bootstrap replicates -- into one summary layer. The
spread statistics are the point: a projection map without one says only what
the ensemble guessed, not how much the members disagreed.
}
\details{
\code{na.rm} defaults to \code{FALSE}, which is the opposite of most R summaries and
is deliberate. If one member is missing over part of the domain, taking the
spread of the members that remain reports a \emph{narrower} uncertainty exactly
where the ensemble is least complete, and nothing on the resulting map says
so. Leaving those cells \code{NA} makes the gap visible. Set \code{na.rm = TRUE} only
once you know why the members differ in coverage.

\code{"cv"} is the standard deviation over the mean. It is the natural choice
when members are on a count or density scale, where a spread of 5 means
something very different at a mean of 10 than at a mean of 1000. It is a
poor choice for probabilities, where the mean can approach zero and the
ratio explodes; use \code{"sd"} there.
}
\examples{
if (requireNamespace("terra", quietly = TRUE)) {
  r <- terra::rast(nrows = 20, ncols = 20, vals = runif(400))
  ensemble <- c(r, r * 1.2, r * 0.7)
  names(ensemble) <- c("model1", "model2", "model3")

  ensemble_summary(ensemble, "sd")
}

}
\seealso{
\code{\link[=plotUncertainty]{plotUncertainty()}} to draw it, \code{\link[=mess]{mess()}} for whether the projection
is extrapolating in the first place.

Other spatial plots:
\code{\link[=hex_bin]{hex_bin()}},
\code{\link[=mess]{mess()}},
\code{\link[=plotExtrapolation]{plotExtrapolation()}},
\code{\link[=plotHexbin]{plotHexbin()}},
\code{\link[=plotUncertainty]{plotUncertainty()}}
}
\concept{spatial plots}
