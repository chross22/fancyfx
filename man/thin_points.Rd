% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_bias.R
\name{thin_points}
\alias{thin_points}
\title{Thin points so that no cell holds more than a few}
\usage{
thin_points(
  x,
  coords = NULL,
  n = 1,
  cellsize = NULL,
  bins = 20,
  type = c("hex", "grid"),
  seed = 1
)
}
\arguments{
\item{x}{A data frame of points.}

\item{coords}{The two coordinate columns. Guessed when omitted.}

\item{n}{Maximum number of points to keep per cell.}

\item{cellsize}{Cell size in the units of the coordinates. Ignored when
\code{bins} is used.}

\item{bins}{Approximate number of cells across the x range, as an
alternative to naming a \code{cellsize}.}

\item{type}{\code{"hex"} for a hexagonal lattice, or \code{"grid"} for a square one.}

\item{seed}{Random seed, since which points survive is a random choice among
those sharing a cell.}
}
\value{
The rows of \code{x} that survive thinning, in their original order.
}
\description{
Reduces the effect of uneven survey effort. Where records pile up because
somewhere was visited often rather than because the species is common there,
a model fitted to the raw points learns the sampling as though it were the
species.
}
\details{
Thinning is a blunt instrument and it throws data away. It is worth doing
when the clustering is an artefact of where people looked, and worth \emph{not}
doing when the clustering is the signal -- there is no way for the function
to tell which, so the judgement stays with you.

The hexagonal lattice is the default for the same reason \code{\link[=hex_bin]{hex_bin()}} uses
one: its cells have neighbours all at equal distance, where a square grid
does not, so thinning is not subtly directional.
}
\examples{
set.seed(1)
# Heavily oversampled in one corner
records <- data.frame(
  x = c(runif(400, 0, 2), runif(100, 0, 10)),
  y = c(runif(400, 0, 2), runif(100, 0, 10))
)

nrow(records)
nrow(thin_points(records, n = 1, bins = 10))

}
\seealso{
\code{\link[=hex_bin]{hex_bin()}}, which uses the same lattice to summarise rather than
to thin, and \code{\link[=spatial_sorting_bias]{spatial_sorting_bias()}} for the related problem in a
train/test split.

Other spatial plots:
\code{\link[=ensemble_summary]{ensemble_summary()}},
\code{\link[=hex_bin]{hex_bin()}},
\code{\link[=mess]{mess()}},
\code{\link[=niche_equivalency]{niche_equivalency()}},
\code{\link[=niche_overlap]{niche_overlap()}},
\code{\link[=plotExtrapolation]{plotExtrapolation()}},
\code{\link[=plotHexbin]{plotHexbin()}},
\code{\link[=plotUncertainty]{plotUncertainty()}}
}
\concept{spatial plots}
