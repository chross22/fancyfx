% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hexbin.R
\name{hex_bin}
\alias{hex_bin}
\title{Aggregate spatial values into hexagonal bins}
\usage{
hex_bin(
  x,
  value = NULL,
  coords = NULL,
  bins = 30,
  cellsize = NULL,
  fun = c("mean", "median", "sum", "sd", "min", "max", "count"),
  min.n = 1,
  layer = 1
)
}
\arguments{
\item{x}{A \code{SpatRaster}, or a data frame of points.}

\item{value}{For a data frame, the column to summarise. Omit to count points
instead. Ignored for a raster, which uses its first layer unless \code{layer}
says otherwise.}

\item{coords}{For a data frame, the two coordinate columns. Defaults to the
first pair of names found among the usual candidates.}

\item{bins}{Approximate number of hexagons across the x range. Ignored when
\code{cellsize} is given.}

\item{cellsize}{Hexagon size, measured centre to vertex, in the units of the
coordinates. Overrides \code{bins} when supplied.}

\item{fun}{How to summarise the values in each hexagon: \code{"mean"},
\code{"median"}, \code{"sum"}, \code{"sd"}, \code{"min"}, \code{"max"}, or \code{"count"}.}

\item{min.n}{Hexagons holding fewer than this many values are dropped. See
Details.}

\item{layer}{For a raster, which layer to summarise.}
}
\value{
A data frame with one row per hexagon: \code{.x} and \code{.y} for the centre,
\code{.value} for the summary, and \code{.n} for how many values it covers. Carries
a \code{"cellsize"} attribute.
}
\description{
Summarises covariates, model output or raw observations into a hexagonal
lattice. Works on a \code{SpatRaster} or on a data frame of points, and returns
the binned values rather than only a picture, so the result can be analysed,
joined or written out.
}
\details{
Hexagons over squares for a reason worth stating: every neighbour of a
hexagon shares an edge and sits at the same distance, where a square grid
has neighbours at two different distances depending on whether they meet at
an edge or a corner. That makes hexagons better behaved for anything that
depends on adjacency, and it removes the visual grain a square lattice
imposes on a map.

Binning is also a claim about resolution. A projection raster drawn at native
resolution invites the reader to believe every pixel is separately estimated,
which is rarely true when the covariates were interpolated from far coarser
data. Aggregating to a cell size you can defend is more honest than drawing
detail the model does not have.

\code{min.n} exists because a hexagon holding one observation is not a summary of
anything, and on a map it is indistinguishable from one holding a thousand.
The returned \code{.n} column reports the count either way.
}
\section{Coordinates and area}{

The lattice is built in whatever units the coordinates are in. For projected
coordinates that gives equal-area hexagons, which is what you want. For
unprojected longitude and latitude it does not: a degree of longitude
shortens toward the poles, so hexagons at the top of a domain cover less
ground than those at the bottom, and a count per hexagon is not a density.
\code{hex_bin()} says so once per session when handed lon/lat. Project first if
the areas matter.
}

\examples{
set.seed(1)
points <- data.frame(x = runif(500, 0, 10), y = runif(500, 0, 10))
points$catch <- points$x + rnorm(500)

binned <- hex_bin(points, value = "catch", bins = 12)
head(binned)

# Counts rather than a summary of some value
head(hex_bin(points, fun = "count", bins = 12))

}
\seealso{
\code{\link[=plotHexbin]{plotHexbin()}} to draw it.

Other spatial plots:
\code{\link[=ensemble_summary]{ensemble_summary()}},
\code{\link[=mess]{mess()}},
\code{\link[=niche_equivalency]{niche_equivalency()}},
\code{\link[=niche_overlap]{niche_overlap()}},
\code{\link[=plot.fancyfx_equivalency]{plot.fancyfx_equivalency()}},
\code{\link[=plotExtrapolation]{plotExtrapolation()}},
\code{\link[=plotHexbin]{plotHexbin()}},
\code{\link[=plotUncertainty]{plotUncertainty()}},
\code{\link[=thin_points]{thin_points()}}
}
\concept{spatial plots}
