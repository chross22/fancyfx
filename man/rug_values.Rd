% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotRugs.R
\name{rug_values}
\alias{rug_values}
\title{The values a rug is drawn from}
\usage{
rug_values(dat, var)
}
\arguments{
\item{dat}{Raw data.}

\item{var}{A column name, or a term to evaluate in \code{dat}.}
}
\value{
A numeric vector.
}
\description{
A column when \code{var} names one, and otherwise the term evaluated in the data
-- which is what makes a rug possible under \code{s(log10(depth))} or \code{s(I(x^2))}.
Evaluated in the data frame alone, with no enclosing environment, so that a
term naming a column that is not there cannot silently pick up a variable of
the same name from the caller and draw a rug of something else entirely.
}
\keyword{internal}
