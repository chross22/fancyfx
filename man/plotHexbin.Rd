% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hexbin.R
\name{plotHexbin}
\alias{plotHexbin}
\title{Map values aggregated into hexagonal bins}
\usage{
plotHexbin(
  x,
  value = NULL,
  coords = NULL,
  bins = 30,
  cellsize = NULL,
  fun = c("mean", "median", "sum", "sd", "min", "max", "count"),
  min.n = 1,
  layer = 1,
  title = "",
  legend.lab = NULL,
  theme = theme_fancyfx(),
  option = "viridis",
  colour = NA
)
}
\arguments{
\item{x}{A \code{SpatRaster}, a data frame of points, or a frame already returned
by \code{\link[=hex_bin]{hex_bin()}}.}

\item{value}{For a data frame, the column to summarise. Omit to count
points.}

\item{coords}{For a data frame, the two coordinate columns.}

\item{bins}{Approximate number of hexagons across the x range.}

\item{cellsize}{Hexagon size, centre to vertex. Overrides \code{bins}.}

\item{fun}{How to summarise each hexagon; see \code{\link[=hex_bin]{hex_bin()}}.}

\item{min.n}{Hexagons holding fewer than this many values are dropped.}

\item{layer}{For a raster, which layer to summarise.}

\item{title}{Plot title, optional.}

\item{legend.lab}{Legend title. Defaults to naming the summary.}

\item{theme}{A \pkg{ggplot2} theme. Defaults to \code{\link[=theme_fancyfx]{theme_fancyfx()}}.}

\item{option}{Viridis colour map option.}

\item{colour}{Outline colour for each hexagon. \code{NA} for none, which is
usually right at small cell sizes.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Draws the output of \code{\link[=hex_bin]{hex_bin()}}. Useful for showing survey effort or catch
at a resolution the data supports, and for aggregating a projection raster
to a cell size you can defend rather than drawing every pixel as though it
were separately estimated.
}
\details{
The fill is a sequential viridis scale, because a binned summary is a
magnitude. See \code{\link[=hex_bin]{hex_bin()}} for why hexagons rather than squares, what
\code{min.n} is for, and the caveat about binning unprojected coordinates.
}
\examples{
set.seed(1)
points <- data.frame(x = runif(800, 0, 10), y = runif(800, 0, 10))
points$catch <- points$x + rnorm(800)

plotHexbin(points, value = "catch", bins = 14)

# Survey effort: how many observations fall in each hexagon
plotHexbin(points, fun = "count", bins = 14, legend.lab = "Observations")

}
\seealso{
\code{\link[=hex_bin]{hex_bin()}} for the binned values themselves.

Other spatial plots:
\code{\link[=ensemble_summary]{ensemble_summary()}},
\code{\link[=hex_bin]{hex_bin()}},
\code{\link[=mess]{mess()}},
\code{\link[=niche_equivalency]{niche_equivalency()}},
\code{\link[=niche_overlap]{niche_overlap()}},
\code{\link[=plotExtrapolation]{plotExtrapolation()}},
\code{\link[=plotUncertainty]{plotUncertainty()}},
\code{\link[=thin_points]{thin_points()}}
}
\concept{spatial plots}
